services:
  backend:
    build: ./backend
    ports:
      - 4000:4000
    env_file:
      - vars.env
    volumes:
      - ./backend/src:/app/src

  frontend:
    build: ./frontend
    ports:
      - 3000:3000
    volumes:
      - ./frontend/src:/app/src

  mobile:
    build: ./mobile
    ports:
      - 19000:19000
    volumes:
      - ./mobile/App:/App

  db:
    image: postgres
    restart: always
    env_file:
      - vars.env

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

volumes:
  postgres:
