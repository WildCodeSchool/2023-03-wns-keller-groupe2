events {}

http {
  include mime.types;

  server {
    listen 80;

    location /graphql {
      proxy_pass http://backend:4000;
    }

    location / {
      root /web-frontend-build;
      try_files $uri /index.html;
    }
  }
}
